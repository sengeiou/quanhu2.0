<shape
    xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 内部颜色 -->
    <!--<solid-->
        <!--android:color="#ffffff" />-->
    <!-- 边缘线条颜色 -->
    <stroke
        android:width="1dp"
        android:color="@color/white" />
    <!-- 圆角的幅度 -->
    <corners
        android:topLeftRadius="@dimen/px10"
        android:topRightRadius="@dimen/px10"
        android:bottomLeftRadius="@dimen/px10"
        android:bottomRightRadius="@dimen/px10" />

</shape>